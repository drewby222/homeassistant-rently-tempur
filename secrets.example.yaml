# Rently (if you choose to use env vars instead, omit these)
rently_email: "YOUR_EMAIL"
rently_password: "YOUR_PASSWORD"

# Sleeptracker / Tempur
sleeptracker_basic: "Basic BASE64(email:password)"

# Optional: if you store tokens in secrets (not recommended)
# sleeptracker_token: "..."
